{
  "name": "@kit/breaker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Blind breaker: reads only the contract and a live URL, derives falsifying vectors, executes them, and returns a typed verdict.",
  "scripts": {
    "breaker": "tsx src/breaker.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "node-html-parser": "^9.0.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.15",
    "typescript": "~5.9.3"
  }
}
