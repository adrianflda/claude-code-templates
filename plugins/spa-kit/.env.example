# Absolute origin of the deployed site. Every canonical URL, sitemap entry and
# JSON-LD @id derives from it, so a wrong value here silently breaks indexing.
NEXT_PUBLIC_SITE_URL=http://localhost:4321

# Base URL the verification tools point at (Playwright, SEO probes, breaker).
KIT_BASE_URL=http://localhost:4321
