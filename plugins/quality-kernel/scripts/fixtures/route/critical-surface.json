{
  "$comment": "Replay fixture (M1 INV-R5). Globs are scoped to CODE extensions where a bare name would over-match docs (precision), while still catching two REAL past bugs the generic default missed (recall): the PHI/IDOR fix (a monitoring controller/service) and inhaler #560 (realtime/brain/inhaler). NOTE (honesty): INV-R5 proves a surface CAN be authored to catch KNOWN incidents; it does not prove it catches the NEXT unknown one. The disciplined validation is hold-out (author from incident A, validate against unseen incident B) + precision tests — see route.test.mjs.",
  "criticalGlobs": [
    "**/auth/**",
    "**/session/**",
    "**/*tenant*/**",
    "**/voice/**",
    "**/telephony/**",
    "**/migrations/**",
    "**/*.sql",
    "infra/**",
    "**/contracts/**",
    "**/controllers/**",
    "**/inhaler/**",
    "realtime/brain/**",
    "**/*monitoring*.ts",
    "**/*monitoring*.js",
    "**/*session*.ts",
    "**/*session*.js"
  ],
  "safeGlobs": [
    "**/*.md",
    "docs/**",
    "**/*.test.*",
    "**/*.spec.*",
    "**/__tests__/**",
    "**/*.css",
    "**/*.stories.*",
    "scripts/dev/**"
  ]
}
